# 009 — Tasks — MHW tools + Melder

- [x] T1 — Record tool/melder counts + tables/commands/queries/types/routes.
- [x] T2 — Record MHW-only gating (`gameOnly`, layout guard, redirect).
- [x] T3 — Record Melder block + global-search coverage.
- [x] T4 — Run `npm run check` (0 errors, 0 warnings).
- [x] T5 — Run `cargo test --manifest-path src-tauri/Cargo.toml`.
- [x] T6 — Run `cargo build --manifest-path src-tauri/Cargo.toml`.
- [x] T7 — Verify against `spec.md` acceptance criteria (retro-anchor; no code changes).
