# 000 — Tasks — MH2G baseline

- [x] T1 — Record authoritative MH2G counts (items 1083 / sources 12751 / combines 432 / monsters 83 / weapons 1500 / armor 2075+949 sets / quests 610 / skills 99+214 / decos 192).
- [x] T2 — Record game-faithful ordering (Chest / Smith / Hunter's Notes / hubs / Book) + filters.
- [x] T3 — Record detail/ASS/search/combine behavior + `tools` hidden for mh2g.
- [x] T4 — Record known exceptions (armor_sets DELETE+reinsert, combine-breakdown staleness, Small-first SQL vs Notes order, MHW quest offset).
- [x] T5 — Run `npm run check` (0 errors, 0 warnings).
- [x] T6 — Run `cargo test --manifest-path src-tauri/Cargo.toml`.
- [x] T7 — Run `cargo build --manifest-path src-tauri/Cargo.toml`.
- [x] T8 — Verify against `spec.md` acceptance criteria (retro-anchor; no code changes).
